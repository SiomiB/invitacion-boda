import { Routes } from '@angular/router';
import { WeddingInviteComponent } from './pages/wedding-invite/wedding-invite.component';

export const routes: Routes = [
    { path: '', component: WeddingInviteComponent },
];
